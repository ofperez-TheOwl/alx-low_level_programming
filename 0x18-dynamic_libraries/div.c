#include "operations.h"

/**
 * div - divide a number by another
 * @a: int, first number
 * @b: int, second number
 *
 * Return: int, result of a / b
 * TheOwl
 */
int div(int a, int b)
{
	if (b != 0)
		return (a / b);
}
