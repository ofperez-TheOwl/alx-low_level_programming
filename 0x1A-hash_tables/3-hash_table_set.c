#include "hash_tables.h"

/**
 * hash_table_set - adds an element to the hash table
 * @ht: pointer to hash_table_t; the hash table
 * @key: const char; key of the element to add
 * @value: const char; value of the element to add
 *
 * Return: int; 1 if success and 0 otherwise
 */
int hash_table_set(hash_table_t *ht, const char *key, const char *value)
{
	unsigned long int index;

	/* get the index of key */
	/* create a copy of value */
	/* access index of key */
	/* check if index is free */
